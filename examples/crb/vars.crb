def a = 4;
def b = 5;
def c = "Hello";
def d = true;

printf("a: %, b: %, c: %, d: %\n", a, b, c, d);
println("a + b: " + (a + b));
println("a - b: " + (a - b));
println("a * b: " + (a * b));
println("a / b: " + (a / b) + "\n");

println("a + c: " + (a + c));
println("a + d: " + (a + d.to_int()));