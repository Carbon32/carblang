def content = ul();
const arr = ["Brush teeth", "Eat breakfast", "Go for a walk"];

def i;
for(i = 0; i < arr.length(); i = i + 1)
{
	content = content + li(arr[i]).attr("style", "color: white;");
}

body(
	div(content).attr("style", "background: rgb(48, 48, 48);")
);

print(builder.construct());